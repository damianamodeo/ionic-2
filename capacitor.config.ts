import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.v2',
  appName: 'ionic-2',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
